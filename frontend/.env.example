
# This is an example for frontend/.env.local or frontend/.env
# Use this if you are running the frontend locally (npm run dev)
# and the backend is NOT on http://localhost:3001
# VITE_API_BASE_URL=http://your-backend-host:your-backend-port
